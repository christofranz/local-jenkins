pipeline {
    agent { label 'default' }
    stages {
        stage('build') {
            steps {
                sh 'cd /home/chris/Dokumente/local-jenkins/'
                sh 'ls'
            }
        }
    }
}
